# Miscellaneous
# Windows reserved device name (avoid "failed to insert into database" on git add)
nul
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.ipr
*.iws
.idea/

# Launch config may contain API keys—do not commit
.vscode/launch.json

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Build artifacts and cache files
build/
*.dill
*.dill.track.dill
*.cache.dill

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android/Gradle
android/.gradle/
android/.gradle/**
*.lock
.gradle/
build/
local.properties

# Firebase / Google Services (download from Firebase Console per app)
android/app/google-services.json
android/app/google-services.json

# Local API keys / secrets (never commit real keys)
lib/config/secrets_local.dart
.env
.env.local
config/secrets.json
assets/config/secrets.json
